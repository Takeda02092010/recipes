import 'package:flutter/material.dart';

class DetailsPage extends StatelessWidget {
  const DetailsPage({super.key, required product});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}